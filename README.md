This dirty library takes an image (Frame) and returns the position of the nozzle if any is detected.
the file contains an example test case with pictures of nozzles from the images folder.

the code can be massively improved, but this is only a prototype and as I learn more of OpenCV, I have a feeling that I'll find a better way.
